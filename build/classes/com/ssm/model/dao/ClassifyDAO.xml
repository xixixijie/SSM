<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssm.model.dao.ClassifyDAO">
  <update id="deleteClassify" parameterType="int">
      undate classify
      set classState=0
      WHERE classifyID=#{classifyID}
  </update>

  <insert id="addClassify" parameterType="Classify">
      <selectKey keyProperty="Classify" resultType="int" order="BEFORE">
          SELECT nvl(max(classifyID),0)+1 from classify
      </selectKey>
      INSERT into classify
      values(#{classifyID},#{className},#{classDis},#{classState})
  </insert>

    <select id="getClassify" parameterType="int" resultType="Classify">
        SELECT * FROM classify WHERE classifyID=#{classifyID}
    </select>

    <update id="ModifyClassify" parameterType="Classify">
        UPDATE classify
        set className=#{className},
        classDis=#{classDis}
    </update>

    <select id="showClassify" resultType="Classify">
        SELECT * from classify where classify.classstate=1
    </select>
</mapper>









